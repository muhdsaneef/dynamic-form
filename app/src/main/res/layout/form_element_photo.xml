<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rlv_form_element_photo"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_image_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:text="Title of photo"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="5dp"
        android:layout_alignStart="@+id/img_form_element_photo"
        android:textSize="18sp"/>

    <ImageView
        android:id="@+id/img_form_element_photo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:scaleType="fitXY"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tv_image_title"
        android:layout_marginBottom="15dp"
        android:background="@android:color/holo_blue_light"/>

    <ImageView
        android:id="@+id/img_delete"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:padding="5dp"
        android:src="@drawable/ic_delete"
        android:background="@android:color/white"
        android:layout_alignEnd="@+id/img_form_element_photo"
        android:layout_alignTop="@+id/img_form_element_photo"
        android:layout_marginTop="-15dp"/>

</RelativeLayout>
